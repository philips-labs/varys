#!/usr/bin/env node

"use strict";

const program = require("commander");
const chalk = require("chalk");

const pkg = require("../package.json");
const { infoMessage } = require("../lib/logger/logger");
const { showUsers } = require("../lib/commands/show-users-graphql");
const { defaultConfiguration } = require("../lib/config");

program
  .version(pkg.version)
  .description(pkg.description)
  .action(function(env) {
    infoMessage(chalk`show users`);
    showUsers(defaultConfiguration);
  })
  .parse(process.argv);
